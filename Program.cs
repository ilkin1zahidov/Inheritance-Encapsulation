using Domain.Models;
using Inheritance_Encapsulation;
using Inheritance_Encapsulation.Models;
using System;
using System.Reflection;

namespace Encapsulation
{
    class Program
    {
        static void Main(string[] args)
        {
            //Person person = new Person(24);

            //person.name = "Ilkin";


            //typeof(Person).GetField("surname", BindingFlags.NonPublic | BindingFlags.Instance).SetValue(person, "Zahidov");
            //var personSurname = typeof(Person).GetField("surname", BindingFlags.NonPublic | BindingFlags.Instance).GetValue(person);

            //Console.WriteLine(personSurname);

            //Car car = new Car();
            //car.HorsePower = 120;
            //Console.WriteLine(car.HorsePower);

            //Animal animal = new Animal();

            //Book book = new Book();

            //Methoda musbet bir eded gelir, hemin ededin faktorialinin hesablanmasi algoritmi yazmaq(method ve class istifade etmekl

            //int n = 5;
            //int num = 1;
            //while (num > 0)
            //{

            //    for (int i = n - 1; i > 0; i--)
            //    {
            //        n *= i;
            //    }
            //    Console.WriteLine("Factorial of {0}! = {1}\n", num, n);
            //    num--;
            //}



        }
    }
}
